<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bloodbank">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="activities.SearchBloodActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name="activities.TableViewDonorsInfoActivity" />
        <activity android:name="activities.BCollectorRegtrActivity" />
        <activity android:name="activities.BCollectorLogActivity" />
        <activity android:name="activities.CollectorLogRegActivity" />
        <activity android:name="activities.CollectBloodActivity" />
        <activity android:name="activities.HomeActivity" />
        <activity android:name="activities.DonorInfoActivity" />
        <activity android:name="activities.SplashScreen">


        </activity>
        <activity android:name="activities.MainActivity" />
    </application>

</manifest>